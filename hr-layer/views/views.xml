<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="hr-layer.list">
      <field name="name">hr-layer list</field>
      <field name="model">hr-layer.hr-layer</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="hr-layer.action_window">
      <field name="name">hr-layer window</field>
      <field name="res_model">hr-layer.hr-layer</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="hr-layer.action_server">
      <field name="name">hr-layer server</field>
      <field name="model_id" ref="model_hr-layer_hr-layer"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="hr-layer" id="hr-layer.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="hr-layer.menu_1" parent="hr-layer.menu_root"/>
    <menuitem name="Menu 2" id="hr-layer.menu_2" parent="hr-layer.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="hr-layer.menu_1_list" parent="hr-layer.menu_1"
              action="hr-layer.action_window"/>
    <menuitem name="Server to list" id="hr-layer" parent="hr-layer.menu_2"
              action="hr-layer.action_server"/>
    -->
  </data>
</odoo>